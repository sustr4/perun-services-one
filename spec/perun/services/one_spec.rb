require "spec_helper"

describe Perun::Services::One do
  it "has a version number" do
    expect(Perun::Services::One::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
